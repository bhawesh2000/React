import React from 'react'

function Column() {
  return (
    <> {/* <> or <React.Fragment> is fragment helps to remove extra node so we replace div tag with fragment */}
    <td>Name</td>
    <td>Bhawesh</td>
    </>
  )
}

export default Column
